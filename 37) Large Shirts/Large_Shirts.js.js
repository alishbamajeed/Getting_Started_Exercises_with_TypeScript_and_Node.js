"use strict";
function make_shirt(size, message) {
    console.log(`The shirt size is ${size} and it will be printed with the following message: "${message}".`);
}
// Calling the function
make_shirt("Large", "I love TypeScript!");
